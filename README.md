# Inventory_management_system
This is a Python-based Inventory Management System that uses a custom-built file handling library (txt_file_lib.py) to perform read and write operations on text files. The system allows users to manage inventory items efficiently and generate PDF bills with unique invoice numbers.
